import React from 'react';
import { Icon } from '@ui-kitten/components';

export const audioIcon = (style) => (
    <Icon {...style} name='mic' />
);

export const audioOffIcon = (style) => (
    <Icon {...style} name='mic-off' />
);