import React from 'react';
import { Icon } from '@ui-kitten/components';

export const videoIcon = (style) => (
    <Icon {...style} name='video' />
);

export const videoOffIcon = (style) => (
    <Icon {...style} name='video-off' />
);