import React from 'react';
import {Icon} from '@ui-kitten/components';

export const frontCameraIcon = (style) => (
  <Icon {...style} name="camera-outline" />
);

export const backCameraIcon = (style) => <Icon {...style} name="camera" />;
